import { withStyles } from "@material-ui/core/styles";

export default withStyles((theme) => {
    return {
        container: {
            height: "calc(100vh - 60px)",
            width: "100vw"
        }
    };
});
